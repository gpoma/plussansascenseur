{% extends 'base.html.twig' %}

{% form_theme form _self 'bootstrap_4_layout.html.twig' %}

{% block body %}

<h2>Rejoindre la communauté de votre ascenseur</h2>

<p class="text-muted">Pour montrer le nombre d'usagers impactés par la panne de cet ascenseur, n'hésitez pas à rejoindre la communauté de votre ascenseur. Si vous le souhaitez, vous pourrez également être tenu informé de l'évolution de la situation.</p>

{{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.pseudo) }}
        {{ form_widget(form.pseudo) }}
        <small class="form-text text-muted">Votre nom / pseudo sera rendu public (la saisie est facultative)</small>
    </div>
    <div class="form-group">
        {{ form_widget(form.abonnement) }}
    </div>
    <div id="signalement_infos_abonnement" class="{% if not form.vars.value.abonnement %}d-none{% endif %}">
    {{ form_row(form.email) }}
    {{ form_row(form.telephone) }}
    <small class="form-text text-muted">L'email et le téléphone sont des données à caractère personnelles qui seront utilisées que dans le cadre de ce service. Elles ne seront pas communiquées à des tiers. Vous disposez d'un droit d'accès, de modification et de suppression conformément au dispositions CNIL. N'hésitez pas à contacter les administrateurs du site pour toute question.</small>
    </div>
    <hr />
    <div class="row">
    <div class="col-6">
        <a href="{{ path('ascenseur', {'id': ascenseur.id}) }}" class="btn btn-block btn-outline-secondary">Annuler</a>
    </div>
    <div class="col-6">
        <button type="submit" class="btn btn-block btn-success">Rejoindre la communauté</button>
    </div>
    </div>
{{ form_end(form) }}
{% endblock %}
