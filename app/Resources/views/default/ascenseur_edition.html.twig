{% extends 'base.html.twig' %}

{% form_theme form _self %}

{% block form_label_class -%}
col-sm-6 col-md-4
{%- endblock form_label_class %}

{% block form_group_class -%}
col-sm-6 col-md-8
{%- endblock form_group_class %}

{% block body %}
<h2>Compléter les informations de l'ascenseur</h2>
<p>N'hésitez pas à vous appuyer sur les photos ci-dessous</p>
<hr />
{% for photo in ascenseur.photos|slice(0, 4) %}
    <a href=""><img src="{{ photo.getBase64Src() }}" alt="{{ photo.updatedAt |date("m/d/Y") }}" class="img-fluid" style="max-height: 150px;"/></a>
{% endfor %}
<hr />
{{ form_start(form) }}
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Localisation de l'ascenseur</3>
  </div>
  <div class="panel-body">
  {{ form_row(form.adresse) }}
  {{ form_row(form.commune) }}
  {{ form_row(form.codePostal) }}
  {{ form_row(form.emplacement) }}
  </div>
</div>

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Informations techniques</h3>
  </div>
  <div class="panel-body">
  {{ form_row(form.constructeurNom) }}
  {{ form_row(form.reference) }}
  {{ form_row(form.dateConstruction) }}
  {{ form_row(form.dateRenovation) }}
  {{ form_row(form.etageMin) }}
  {{ form_row(form.etageMax) }}
  </div>
</div>

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Contact des responsables</h3>
  </div>
  <div class="panel-body">
  {{ form_row(form.bailleurNom) }}
  {{ form_row(form.bailleurContact) }}
  {{ form_row(form.syndicNom) }}
  {{ form_row(form.syndicContact) }}
  {{ form_row(form.ascensoristeNom) }}
  {{ form_row(form.ascensoristeContact) }}
  {{ form_row(form.telephoneDepannage) }}
  </div>
</div>


<hr />
<div class="row">
    <div class="col-6">
        <a href="{{ path('ascenseur', {'id': ascenseur.id}) }}" class="btn btn-block btn-light">Annuler</a>
    </div>
    <div class="col-6">
        <button type="submit" class="btn btn-block btn-success">Enregistrer</button>
    </div>
</div>
{{ form_end(form) }}
{% endblock %}
